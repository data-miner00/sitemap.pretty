/**
 * Prefix two inputs with 'this'.
 * @param {string} input1
 * @param {string} input2
 * @returns The string prefixed with 'this'.
 */
function prefixWithThis(input1, input2) {
  return compileInputs("this", input1, input2);
}
